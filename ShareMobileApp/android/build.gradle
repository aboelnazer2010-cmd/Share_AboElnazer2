apply plugin: "com.android.application"
apply plugin: "com.facebook.react"

/**
 * إعدادات React Native لنسخة 0.72
 */
react {
    /* لا تضع دالة autolinkLibrariesWithApp هنا لأنها غير موجودة في إصدارك */
}

android {
    // حل مشكلة "compileSdkVersion is not specified"
    compileSdkVersion rootProject.ext.compileSdkVersion
    
    namespace "com.shareaboelnazer"
    defaultConfig {
        applicationId "com.shareaboelnazer"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.0"
    }
    
    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.debug // للتجربة فقط
        }
    }
}

dependencies {
    implementation("com.facebook.react:react-android")
    implementation("com.facebook.react:hermes-android")
}

// الطريقة الصحيحة للربط التلقائي في إصدار 0.72
apply from: file("../../node_modules/@react-native-community/cli-platform-android/native_modules.gradle"); 
applyNativeModulesAppBuildGradle(project)
